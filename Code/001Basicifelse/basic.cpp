/*
g++ <name of the file> --> to compile the code. [but should be in the same directory so that code file is able to fetch]
cd <name of the file > --> to cahnge the firectory forward.
cd .. to go back to directory
g++ __. to compile
./a --> to run the file
ctrl + c  __. to kill the terminal

to make folder --> mkdir <folderName>
to make file --> type nul > <fileName>
to view the files or folder in any folder --> ls
*/
#include <iostream>
using namespace std;

int main()
{
cout << "noting";
return 0;
}
